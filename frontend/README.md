
```markdown
// filepath: [README.md](http://_vscodecontentref_/136)
// ...existing code...
# Frontend — VIDUR (React + Vite)

Quick start:

```bash
cd frontend
npm install
npm run dev
# open http://localhost:8080
```

## Notes:

- Dev server proxy configured in vite.config.ts to forward /api to the backend.
- API client and token helpers: api.ts
- File upload helper: fileUpload.ts
- Export utilities: export.ts
- Build for production:

```bash
npm run build
npm run preview
```

